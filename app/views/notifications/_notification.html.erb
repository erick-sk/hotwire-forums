<div id="<%= dom_id(notification) %>" class="flex my-2">
  <% noticed_notification = notification.to_notification %>

  <%= link_to noticed_notification.message, noticed_notification.url, class: "underline text-blue-700 mr-2" %> | <%= notification.created_at %>
</div>